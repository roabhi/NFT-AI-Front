export const formSubmitBtn = document.getElementById('form-submit'),
  selectBtn = document.getElementById('select-btn'),
  hiddenFileSelector = document.getElementById('myFile'),
  selectedFilename = document.getElementById('selected_filename'),
  spinner = document.querySelector('div.spinner'),
  imagesHolder = document.querySelector('div.images-holder'),
  prompter = document.getElementById('prompt')
